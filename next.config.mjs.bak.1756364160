/** @type {import('next').NextConfig} */
const basePath = '/corwin';

const nextConfig = {
  basePath,
  trailingSlash: true,              // canonical URL will be /corwin/
  env: { NEXT_PUBLIC_BASE_PATH: basePath },
};

export default nextConfig;
